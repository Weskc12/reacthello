

import React from "react";
import reactDOM from "react-dom";

export const ContainerForCards =(props) => {
    return (
        <div className="container">
            <CardsContainer/>
        </div>

    );
};